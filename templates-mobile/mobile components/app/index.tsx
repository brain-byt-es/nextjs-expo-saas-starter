// This screen is now redundant.
// The root layout (_layout.tsx) handles the initial routing logic
// based on the user's authentication state from Clerk.
// We return null while the layout determines the correct route.
export default function Index() {
  return null;
}